# --- Proxy / Webserver -------------------------------------------------
flask==3.1.3
flask-cors==6.0.1
gunicorn==23.0.0

# --- Abhaengigkeiten des vendorten LidlPlus-Clients (main.py + lidl/) ---
# Muessen kompatibel sein mit der pyproject.toml von EvickaStudio/lidl-discounts
# (dependencies: httpx>=0.28.1, pydantic>=2.13.3, pydantic-settings>=2.14.0).
httpx==0.28.1
pydantic==2.13.3
pydantic-settings==2.14.0
